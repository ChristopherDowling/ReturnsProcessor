ReturnsProcessor
================
To use:
-Download and unzip the returns.zip file
-Drag and drop the .csv file on to returns.exe

-Remember to clean up the .csv beforehand (make sure every entry is finished and has no holes, there are no empty lines with random data in them, etc)
